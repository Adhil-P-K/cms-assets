import { uploadRequest } from '../interfaces';

class CatalogService {
  static async saveImage(req: uploadRequest): Promise<any> {
    try {
      const response = '';
      return response;
    } catch (error) {
      throw error;
    }
  }
}
export { CatalogService };
